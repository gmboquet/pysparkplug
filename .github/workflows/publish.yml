name: Publish

# Build the sdist + wheel and publish to PyPI via Trusted Publishing (OIDC) -- no API tokens are
# stored as secrets. Publishing to PyPI runs only when a GitHub Release is published; a manual
# workflow_dispatch can build-and-check (and optionally push to TestPyPI) without touching PyPI.
#
# The PyPI distribution name is "pysp-learn" (the import package is still `pysp`); the GitHub repo
# is gmboquet/pysparkplug.
# One-time setup (PyPI side, done by the maintainer):
#   1. Confirm the project name "pysp-learn" is available or owned by you (https://pypi.org/project/pysp-learn/).
#   2. On PyPI, add a Trusted Publisher for the "pysp-learn" project (a "pending" publisher if it
#      does not exist yet):
#        Owner: gmboquet   Repository: pysparkplug
#        Workflow: publish.yml   Environment: pypi
#      and likewise a TestPyPI pending publisher with Environment: testpypi if you want the test path.
#   3. (Optional) Create GitHub Environments named "pypi" and "testpypi" with any protection rules.
# To release: bump `version` in pyproject.toml, tag the commit (e.g. v0.2.0), and publish a GitHub
# Release for that tag -- this workflow then builds and uploads it.

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      testpypi:
        description: "Also upload to TestPyPI (manual runs only)"
        type: boolean
        default: false

# Cancel superseded runs on the same ref.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: Build sdist + wheel
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Build
        run: |
          python -m pip install --upgrade pip build twine
          python -m build
      # Validate metadata renders on PyPI before any upload.
      - name: Twine check
        run: twine check dist/*
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish-testpypi:
    name: Publish to TestPyPI
    # Manual dry-run path: only when explicitly requested via workflow_dispatch.
    if: github.event_name == 'workflow_dispatch' && inputs.testpypi
    needs: build
    runs-on: ubuntu-latest
    environment: testpypi
    permissions:
      id-token: write # required for trusted publishing (OIDC)
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/

  publish-pypi:
    name: Publish to PyPI
    # Real publish only on a published GitHub Release.
    if: github.event_name == 'release'
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write # required for trusted publishing (OIDC)
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
